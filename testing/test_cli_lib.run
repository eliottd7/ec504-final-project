#!/bin/sh

echo "Running CLI library tests"
mkdir validdir
mkdir validemptydir
touch validdir/validfile.txt
./test_cli_lib.test "validdir" "validdir/validfile.txt" "tmplocker" "validemptydir"
echo Failed $? "test(s)"
rm -rd validdir tmplocker
exit $?
