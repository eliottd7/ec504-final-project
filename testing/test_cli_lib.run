#!/bin/sh

echo "Running CLI library tests"
mkdir validdir
mkdir validemptydir
echo "Hello World" > validdir/validfile.txt
./test_cli_lib.test "validdir/validfile.txt" "validdir" "tmplocker" "validemptydir"
rm -rd validdir validemptydir tmplocker
exit $?
